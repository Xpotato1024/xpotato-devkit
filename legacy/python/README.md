# xpotato-devkit (Python Legacy)

このディレクトリは `xpotato-devkit` の旧 Python 実装（レガシー版）を含んでいます。
新機能の追加は行われず、Rust版（ルートのリポジトリ）に移行されています。

詳細な方針については `docs/legacy/python-legacy.md` （またはメインドキュメント）を参照してください。

## インストールと実行

```bash
uv sync
uv run devkit-py --help
```
